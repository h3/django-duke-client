{% extends "base.html" %}
{% load i18n markup generic_flatblocks frontadmin_tags %}


{% block content %}
<div class="container_24 clearfix">
     <div class="article">
        {% gblock "home-text" for "gblocks.TitleAndText" into "block" %}
        {% frontadmin request block %}
        <h3 class="quote grid_9 alpha">
            {{ block.title|title }}
        </h3>
        <div class="grid_14 omega">
             {{ block.text|markdown }}
         </div>
        {% endfrontadmin %}
     </div>
     <hr class="spacer">
     <div class="article">
         <div class="grid_8 suffix_1 alpha">
             {% gblock "home-sec1" for "gblocks.TitleAndText" into "block" %}
             {% frontadmin request block %}
             <h2>{{ block.title }}</h2>
             {{ block.text|markdown }}
             {% endfrontadmin %}
             <a class="btn right" href="/profil" >{% trans "Read more ..." %}</a>
         </div>
         <div class="home-services grid_5 suffix_1">
             {% gblock "home-sec2" for "gblocks.TitleAndText" into "block" %}
             {% frontadmin request block %}
             <h2>{{ block.title }}</h2>
             {{ block.text|markdown }}
             {% endfrontadmin %}
         </div>
         <div class="grid_8 omega">
             {% gblock "home-sec3" for "gblocks.TitleTextAndImage" into "block" %}
             {% frontadmin request block %}
             <h2>{{ block.title }}</h2>
             <img src="{{ MEDIA_URL}}{{block.image}}" width="280">
             {{ block.text|markdown }}
             {% endfrontadmin %}
         </div>
     </div>
</div>
{% endblock  %}
